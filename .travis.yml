sudo: false

dist: xenial

language: python

python:
  - "3.5"
  - "3.6"
  - "3.7"

env:
  - DJANGO="Django>=1.11,<2.0"
  - DJANGO="Django>=2.0,<2.1"
  - DJANGO="Django>=2.1,<2.2"


install:
  - pip install tox
  - pip install coveralls

script:
  - tox

after_success:
  - coveralls